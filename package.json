{
  "name": "cricket-highlights",
  "version": "1.0.0",
  "private": true,
  "description": "Hop 2 highlight picker. Clips from ScoreSnapshot.last_event only — two hops from cricket-protocol.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
